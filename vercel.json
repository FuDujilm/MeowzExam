{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile=false",
  "buildCommand": "pnpm prisma generate && pnpm next build",
  "devCommand": "pnpm next dev --turbopack -p 3001",
  "regions": ["hkg1"]
}
